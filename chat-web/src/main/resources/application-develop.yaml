server:
  servlet:
    context-path: /chat
  port: 1000

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/elovchat?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: 123456

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 10s
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  mail:
    host: smtp.163.com
    port: 465
    username: n3411520440@163.com
    password: URZsU38LHxkjjmym
    from: n3411520440@163.com
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      mail.smtp.starttls.enable: true

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml

app:
  upload-dir: ./uploads

sa-token:
  token-name: Authorization
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: true